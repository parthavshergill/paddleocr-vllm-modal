[project]
name = "paddle-ocr-test"
version = "0.1.0"
description = "High-throughput table extraction with PaddleOCR-VL on Modal"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "modal>=0.64.0",
]
